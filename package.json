{
  "name": "firecrawl-pool",
  "version": "1.0.0",
  "description": "Multi-key Firecrawl proxy with credit-aware routing, keyless fallback, and 402 retry. Pools multiple API keys and routes to the healthiest.",
  "keywords": ["pi-package", "firecrawl", "mcp", "proxy", "web-scraping"],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/themuuln/firecrawl-pool-proxy.git"
  },
  "pi": {
    "extensions": ["./extensions"],
    "skills": ["./skills"]
  }
}
